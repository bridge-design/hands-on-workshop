{
  "name": "tokens",
  "version": "1.0.0",
  "main": "dist/index.js",
  "license": "MIT",
  "scripts": {
    "build": "mkdirp dist && cpy index.js dist && style-dictionary build --config ./config.js",
    "build:add": "git add dist/ && cross-var git commit -m \"Tokens @ $npm_package_version\"",
    "version": "npm run build && npm run build:add"
  },
  "devDependencies": {
    "cpy-cli": "^3.1.1",
    "style-dictionary": "^2.10.3"
  },
  "dependencies": {
    "cross-var": "^1.1.0",
    "mkdirp": "^1.0.4"
  }
}
