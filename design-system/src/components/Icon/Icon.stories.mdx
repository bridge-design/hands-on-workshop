import { Meta, Canvas, Story } from "@storybook/addon-docs/blocks";

import Icon from ".";
import { colors as themeColors } from "tokens";

<Meta title="Components/Icon" />

## Icon

SVG icons with customizable color and size, mainly to be used inside the buttons

<Canvas>
  <Story name="Default">
    {[
      "heart",
      "arrowBack",
      "settings",
      "menu",
      "viewGrid",
      "viewItem",
      "filter",
    ].map((icon) => (
      <Icon key={icon} name={icon} style={{padding: "1em"}} />
    ))}
  </Story>
</Canvas>

<Canvas>
  <Story name="Colored">
    <div>
      {[
        themeColors.primaryOrange100,
        themeColors.primaryPurple100,
        themeColors.secondaryYellow100,
        themeColors.secondaryGreen100,
      ].map((color) => {
        return (
          <div key={color} style={{ width: "100%" }}>
            {[
              "heart",
              "arrowBack",
              "settings",
              "menu",
              "viewGrid",
              "viewItem",
              "filter",
            ].map((icon) => (
              <Icon
                key={icon}
                name={icon}
                color={color}
                style={{padding: "1em"}} 
              />
            ))}
          </div>
        );
      })}
    </div>
  </Story>
</Canvas>
