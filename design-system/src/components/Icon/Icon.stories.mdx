import { Meta, Canvas, Story } from "@storybook/addon-docs/blocks";

import Icon from ".";
import { colors as themeColors } from "tokens";

<Meta title="Icon" />

## Icon

SVG icons with customizable color and size, mainly to be used inside the buttons

<Canvas>
  <Story name="Default">
    <Icon name="heart" />
    <Icon name="settings" />
    <Icon name="arrowBack" />
    <Icon name="menu" />
  </Story>
</Canvas>

<Canvas>
  <Story name="Colored">
    <div>
      {[
        themeColors.primaryOrange100,
        themeColors.primaryPurple100,
        themeColors.secondaryYellow100,
        themeColors.secondaryGreen100,
      ].map((color) => {
        return (
          <div key={color} style={{ width: "100%" }}>
            {["heart", "arrowBack", "settings", "menu", "viewGrid", "viewItem", "filter"].map((icon) => (
              <Icon key={icon} name={icon} color={color} />
            ))}
          </div>
        );
      })}
    </div>
  </Story>
</Canvas>
